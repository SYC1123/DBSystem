<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.dbsystem">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Controller.ReportActivity"></activity>
        <activity android:name=".Controller.ManagerActivity" />
        <activity android:name=".Controller.AdminActivity" />
        <activity android:name=".Controller.MyCollectionActivity" />
        <activity android:name=".Controller.PayActivity" />
        <activity android:name=".Controller.PlaceDownActivity" />
        <activity android:name=".Controller.ChangeInfor" />
        <activity android:name=".Controller.DetailsActivity" />
        <activity android:name=".Controller.PalceUpActivity" />
        <activity android:name=".Controller.LoginActivity" />
        <activity android:name=".Controller.RegisteredActivity" />
        <activity
            android:name=".Controller.UserActivity"
            android:label="@string/title_activity_user"
            android:launchMode="singleTask" />
        <activity android:name=".Controller.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>